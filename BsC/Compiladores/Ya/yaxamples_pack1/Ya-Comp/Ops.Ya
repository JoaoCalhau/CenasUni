@Ops.Ya

`Int +-| %=+ `Int b
	Return -b
`Int `Int a |-+ %>.
	Return -a
`No `Int a <-> %== `Int b
	`Int t = a
	a = b
	b = t
`Int `Int a <=> `Int b
	If a > b
		Return 1
	Else if b > a
		Return -1
	Else
		Return 0

`Int x, y, z
x = +-|2
y = 1|-+
x <-> y
z = x <=> y
//z = (x <=> y)

`Int <( `Int a )>
	Return a
`Int `Int a <[ `Int b ]>
	Return a+b

<(5)>
x = <(5)>
3<[8]>
y = 3<[8]>
